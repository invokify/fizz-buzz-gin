CREATE TABLE IF NOT EXISTS stats (
    id INTEGER NOT NULL PRIMARY KEY,
    createdat TEXT NOT NULL,
    str1 TEXT,
    str2 TEXT,
    int1 INTEGER,
    int2 INTEGER,
    lim INTEGER,
    result TEXT
);